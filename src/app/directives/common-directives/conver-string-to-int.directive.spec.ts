import { ConverStringToIntDirective } from './conver-string-to-int.directive';

describe('ConverStringToIntDirective', () => {
  it('should create an instance', () => {
    const directive = new ConverStringToIntDirective();
    expect(directive).toBeTruthy();
  });
});
