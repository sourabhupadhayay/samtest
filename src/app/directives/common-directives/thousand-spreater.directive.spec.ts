import { ThousandSpreaterDirective } from './thousand-spreater.directive';

describe('ThousandSpreaterDirective', () => {
  it('should create an instance', () => {
    const directive = new ThousandSpreaterDirective();
    expect(directive).toBeTruthy();
  });
});
